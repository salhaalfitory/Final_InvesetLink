@* @using InvestLink_BLL.Models
@model List<UserInRoleVM>;
@{
    ViewBag.Title = "AddOrRemove";
    var RoleId = ViewBag.RoleId;
}

        <div  class="card">
            <div class="card-body">
                <div class="border p-3 rounded">
                    <h6 class="mb-0 text-uppercase">إضافة او إزالة صلاحية</h6>
                    <hr>
            <h3>@ViewBag.Name</h3>

            <form asp-controller="Role" asp-action="AddOrRemove" class="row g-3">
                   @for(int i = 0; i < Model.Count;i++)
                    {
                   
                    <div class="form-check">

                        <input asp-for="@Model[i].IsSelected" type="checkbox" class="form-check-input" value="" id="flexCheckDefault" />
                        <label asp-for="@Model[i].IsSelected" class="form-check-label" for="flexCheckDefault">@Model[i].UserName</label>
                    <br />
                    <br />
                    <input type="hidden" asp-for="@Model[i].UserName" />
                    <input type="hidden" asp-for="@Model[i].Id" />
                    </div>}   
                    
                        <div class="col-12">
                            <div class="d-grid">

                        <input type="submit" class="btn btn-primary" value="Update Role" />

                    </div>
                        </div>
                    </form>
                    <br />
            <a asp-controller="Role" asp-action="Update" asp-route-Id="@RoleId" class="btn btn-success waves-effect">Cancel</a>

                </div>
            </div>
        </div> *@

@using InvestLink_BLL.Models
@model List<UserInRoleVM>;
@{
    ViewBag.Title = "AddOrRemove";
    var RoleId = ViewBag.RoleId;
}

<div class="card">
    <div class="card-body">
        <div class="border p-3 rounded">
            <h6 class="mb-0 text-uppercase">إضافة او إزالة صلاحية</h6>
            <hr>
            <h3>@ViewBag.Name</h3>
            <form asp-controller="Role" asp-action="AddOrRemove" method="post" class="row g-3">
                <input type="hidden" name="RoleId" value="@ViewBag.RoleId" />
                @for (int i = 0; i < Model.Count; i++)
                {

                    <div class="form-check">

                        <input asp-for="@Model[i].IsSelected" type="checkbox" class="form-check-input" id="flexCheckDefault" />
                        <label asp-for="@Model[i].IsSelected" class="form-check-label" for="flexCheckDefault">@Model[i].UserName</label>
                        <br />
                        <br />
                        <input type="hidden" asp-for="@Model[i].UserName" />
                        <input type="hidden" asp-for="@Model[i].Id" />
                    </div>
                }


                <div class="col-12">
                    <div class="d-grid">


                        <input type="submit" value="تعديل صلاحية" class="btn btn-primary" />


                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


@* 
@using InvestLink_BLL.Models
@model List<UserInRoleVM>

@{
    ViewBag.Title = "AddOrRemove";
    var RoleId = ViewBag.RoleId;
}

<div class="card">
    <div class="card-body">
        <div class="border p-3 rounded">
            <h6 class="mb-0 text-uppercase">إضافة أو إزالة صلاحية</h6>
            <hr>
            <h3>@ViewBag.Name</h3>

            <form asp-controller="Role" asp-action="AddOrRemove" method="post" class="row g-3">

                <input type="hidden" name="RoleId" value="@ViewBag.RoleId" />

                @for (int i = 0; i < Model.Count; i++)
                {
                    <div class="form-check m-2">
                        <input asp-for="@Model[i].IsSelected" class="form-check-input" id="check_@i" />

                        <label class="form-check-label" for="check_@i">
                            @Model[i].UserName
                        </label>

                        <input type="hidden" asp-for="@Model[i].UserName" />
                        <input type="hidden" asp-for="@Model[i].Id" />
                    </div>
                }

                <div class="col-12 mt-3">
                    <div class="d-grid">
                        <input type="submit" value="تعديل الصلاحية" class="btn btn-primary" />
                    </div>
                </div>
            </form>

            <div class="mt-3">
                <a asp-controller="Role" asp-action="Update" asp-route-Id="@RoleId" class="btn btn-secondary">إلغاء</a>
            </div>
        </div>
    </div>
</div> *@