@model IEnumerable<InvestLink_BLL.Models.LicenseVM>
@{
    ViewBag.Title = "Index ";
}









<h6 class="mb-0 text-uppercase">الطلبات </h6>
<hr>
<div class="card">
    <div class="card-body">
        <table class="table mb-0 table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">اسم</th>
                    <th scope="col">تاريخ إنشاء</th>
                    <th scope="col">تفاصيل</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int Count = 0;
                }
                @foreach (var Item in Model)
                {
                    Count++;
                    <tr>
                        <th scope="row">@Count</th>

                        <td>@Item.</td>
                        <td>@Item.</td>
                        <td>
                            <div class="col">
                                <a asp-controller="License" asp-action="Details" asp-route-Id=@Item.Id class="btn btn-secondary px-5">
                                    <ion-icon name="document-text-sharp"></ion-icon> تفاصيل
                                </a>
                            </div>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>