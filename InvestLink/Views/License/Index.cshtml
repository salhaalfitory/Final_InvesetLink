@using InvestLink_BLL.Interfaces
@using InvestLink_DAL.Entities
@using Microsoft.AspNetCore.Identity


@{
    ViewBag.Title = "Index ";
}

@inject SignInManager<IdentityUser> signInManager
@inject IProject iproject;
@inject IProjectInvestor iprojectInvestor
@inject IInvestor iInvestor
@inject ILicense iLicense
@inject UserManager<IdentityUser> userManager
@{
    ViewBag.Title = "Index ";
}

@{
    var user = await userManager.GetUserAsync(signInManager.Context.User);
    var email = await signInManager.UserManager.GetEmailAsync(user);
    IEnumerable<License> licenses;
    @if (User.IsInRole("Investor"))
    {
        var investor = await iInvestor.GetByEmailAsync(email);
        var investorId = investor.Id;
        var projectInvestor = await iprojectInvestor.GetAllAsync(investorId);
       var project = await iproject.GetAllAsync(projectInvestor);
         licenses = await iLicense.GetAllAsync(project);


    }
    else
    {
        licenses = await iLicense.GetAllAsync();
    }
}


<h6 class="mb-0 text-uppercase">رخص المشاريع</h6>
<hr>
<div class="card">
    <div class="card-body">
        <table class="table mb-0 table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">الاسم</th>
                    <th scope="col">رقم ترخيص</th>
                    <th scope="col">تاريخ  إنشاء</th>
                    <th scope="col">تاريخ إنتهاء  </th>
                    <th class="text-center">حالة الرخصة</th>
                   
                </tr>
            </thead>
            <tbody>
                @{
                    int Count = 0;
                }
                @foreach (var Item in licenses)
                {
                    Count++;
                    <tr>
                        <th scope="row">
                            <div style="width: 40px; height: 40px; background: linear-gradient(135deg, rgba(79, 172, 254, 0.1) 0%, rgba(0, 242, 254, 0.1) 100%); border-radius: 10px; display: flex; align-items: center; justify-content: center; font-weight: 700; color: #4facfe;">
                                @Count
                            </div>
                        </th>
                        <td>@Item.Project?.Name</td>
                        <td>@Item.LicenseNumber</td>
                        <td>@Item.CreatedDate</td>
                        <td>@Item.ExpireDate</td>  
                        @if (Item.ExpireDate > DateTime.Now)
                        {
                            <td class="align-middle text-center">
                                <span class="badge bg-success text-white py-2 px-3 rounded-3 shadow-sm"
                                      style="min-width: 100px; font-size: 0.9rem; font-weight: normal;">
                                    سارية
                                </span>
                            </td>
                        }
                                        else
                        {
                            <td class="align-middle text-center">
                                <span class="badge bg-danger text-white py-2 px-3 rounded-3 shadow-sm"
                                      style="min-width: 100px; font-size: 0.9rem; font-weight: normal;">
                                    منتهية
                                </span>
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


